# Environment Variables Template for Employee Lifecycle Management System
#
# IMPORTANT: This is a template file. To use it:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env and replace placeholder values with actual credentials
# 3. Never commit .env file to version control (it's in .gitignore)

# Database Configuration
# Set a secure password for the PostgreSQL database
DB_PASSWORD=your_secure_password_here

# SMTP Configuration (Gmail)
# Use a Gmail app-specific password for email functionality
# To create an app password: Google Account > Security > 2-Step Verification > App passwords
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=gmail_app_specific_password
